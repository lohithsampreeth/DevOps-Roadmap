## Project Name

### What it does
Automated CI/CD pipeline that builds, tests and deploys a Dockerized app to AWS EKS.

### Tech Stack
- GitHub Actions, Docker, Terraform, Kubernetes (EKS), Prometheus

### Architecture
[Add a simple diagram or screenshot]

### How to Run
1. Clone the repo
2. Configure AWS credentials
3. Run `terraform init && terraform apply`
